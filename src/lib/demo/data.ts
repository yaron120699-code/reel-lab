/**
 * Kept as the stable import path for demo data. The dataset itself now lives in
 * `fixtures.ts`, which also owns the hardcoded ids the serverless seed depends
 * on.
 */
export {
  DEMO_APIFY_ITEMS,
  DEMO_APIFY_JSON,
  DEMO_COMPETITOR,
  DEMO_COMPETITOR_ID,
  demoReelId,
} from "./fixtures";
